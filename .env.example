# API mode only. Keep the real key in .env on this PC.
OPENAI_API_KEY=
# Set a model ID available to your API project, or enter it in the sidebar.
OPENAI_MODEL=
# Optional absolute path to codex.exe if it is not on PATH.
CODEX_BIN=
# Codex mode uses a separate local home. Run npm run codex:login once.
# Fixed extension endpoint: http://127.0.0.1:4318
